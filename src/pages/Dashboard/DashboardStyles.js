import styled from "styled-components";

export const DashboardContainer = styled.div`
  display: flex;
  flex-direction: row;
  padding: 20px 20px;

  .leftbarWrapper {
    flex: 2;
  }

  .contentWrapper {
    flex: 5;
  }

  .rightbarWrapper {
    flex: 2;
  }
`;
